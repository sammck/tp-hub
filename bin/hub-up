#!/bin/bash

set -e

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )
. "$SCRIPT_DIR/hub-env"

cd "$PROJECT_DIR/traefik"
docker-compose up -d
cd "$PROJECT_DIR/portainer"
docker-compose up -d

echo "success!" >&2
